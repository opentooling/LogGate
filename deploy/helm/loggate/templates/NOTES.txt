LogGate {{ .Chart.AppVersion }} is installed as release {{ .Release.Name }}.

{{ if .Values.ingress.enabled -}}
  URL: http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}
{{- else -}}
  No ingress configured. Port-forward instead:
    kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "loggate.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}

Verify the release:
  helm test {{ .Release.Name }} -n {{ .Release.Namespace }}
