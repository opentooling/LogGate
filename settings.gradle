plugins {
  // Lets Gradle download the Java 25 toolchain on demand, so the build does not
  // depend on which JDKs happen to be installed on the machine.
  id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'loggate'

include 'backend'
